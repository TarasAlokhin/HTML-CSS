// Теорема Піфагора
// var points = [{ x: 0, y: 0 }, { x: 1, y: 1 }];

// points.dist = function() {
//   var p1 = this[0];
//   var p2 = this[1];
//   var a = p2.x - p1.x;
//   var b = p2.y - p1.y;
//   return Math.sqrt(a * a + b * b);
// };
// console.log(points.dist()); // відстань між двома точками

// Функція яка визначає абсолютне значення (модуль числа)
/*var x = -5;
function abs(x) {
  if (x >= 0) {
    return x;
  } else {
    return -x;
  }
}
console.log(abs(x));*/

// Знаходження факторіалу числа

/*var n = 3;

function factorial(n) {
  var product = 1;

  while (n > 1) {
    product *= n;
    n--;
  }

  return product;
}

console.log(factorial(n));*/
